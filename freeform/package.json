{
  "name": "@local/freeform",
  "version": "0.1.0",
  "description": "Build forms, collect submissions, and send notifications directly from EmDash.",
  "type": "module",
  "scripts": {
    "build": "tsdown src/index.ts src/sandbox-entry.ts --format esm --outDir dist --dts false",
    "build:watch": "tsdown src/index.ts src/sandbox-entry.ts --format esm --outDir dist --dts false --watch",
    "bundle": "emdash plugin bundle",
    "bundle:validate": "emdash plugin validate",
    "typecheck": "tsc --noEmit"
  },
  "exports": {
    ".": "./dist/index.js",
    "./sandbox": "./dist/sandbox-entry.js"
  },
  "peerDependencies": {
    "emdash": "^0.12.0"
  },
  "devDependencies": {
    "tsdown": "^0.12.0"
  }
}
